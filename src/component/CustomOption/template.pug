template(id="tpl-select-field")
	style(type="text/css").
		figure {
			margin: 0;
		}
	figure
		slot(name="picture")
		figcaption
			slot
			h4
				slot(name="label")
			div
				slot(name="description")