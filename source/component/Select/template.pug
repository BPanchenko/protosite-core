style(
	title="UIKit Rules"
	type="text/css"
)
	:css-loader(module="assets/component.select.css")
style(type="text/css").
	:host(:state(--defined)) {
		content-visibility: hidden;
	}
	:host(:state(--loaded)) {
		content-visibility: visible;
	}
div#button(
	aria-controls="listbox"
	role="button"
	tabindex=0
)
	div#status(
		aria-placeholder="Выбрать..."
		part="selectedcontent"
		role="status"
	)
e-listbox#picker(
	aria-labelledby="button"
	part="listbox"
	tabindex=0
)
	slot