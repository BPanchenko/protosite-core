export enum ComponentState {
	Animation = '--animating',
	Defined = '--defined',
	Interactive = '--interactive',
	Loaded = '--loaded',
}

export enum ComboboxState {
	Collapsed = 'collapsed',
	Expanded = 'expanded',
}

export enum FieldState {
	Disabled = 'disabled',
}
