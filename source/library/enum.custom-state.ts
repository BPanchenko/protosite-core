const enum CustomState {
	Animation = '--animating',
	Collapsed = '--collapsed',
	Defined = '--defined',
	Disabled = '--disabled',
	Expanded = '--expanded',
	Interactive = '--interactive',
	Loaded = '--loaded',
	Scrolled = '--scrolled',
}

export default CustomState
